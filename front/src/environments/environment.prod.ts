export const environment = {
  production: true,
  // Esta URL se reemplazará durante el despliegue en Vercel
  apiBaseUrl: process.env['NG_APP_API_BASE_URL'] || 'https://learning-cards-a35l.onrender.com/api'
};
